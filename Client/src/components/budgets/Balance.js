import React, { Fragment } from 'react';

const Balance = ({ items, outcome }) => {
	return (
		<Fragment>
			<div className='budget-list' >Balance</div>
		</Fragment>
	);
};

export default Balance;
